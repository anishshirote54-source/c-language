#include <stdio.h>
int main ()
{
    int a, b;
    a = 30;
    b = 10;
    printf("a + b = %d", a+b);
    printf("a - b = %d", a-b);
    printf("a * b = %d", a*b);
    printf("a / b = %d", a/b);
    
    return 0;
    
}